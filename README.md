# Contents for my personal website

Jekyll website with a `minima` theme.

## Things that are different:

**Added Academicons**

- Added `css/` and `fonts/` to `assets/`.
- Edited `_includes/head.html` to reflect this.
